<form action="/plant/list">
    <input type="search" name="q" required>
    <input type="submit" value="search">
</form>
{{#each plants}}

<h4>{{common_name}}</h4>


<img src="{{default_image.medium_url}}" alt="">
<button onclick="addFav(event,{{id}})">
    add Fav
</button>
{{/each}}